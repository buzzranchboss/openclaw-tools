# Session Memory — 2026-04-05 (Afternoon)

## What We Did

- **Reddit r/vibecoding post removal** — post removed by mods (not a ban this time). Root cause: "link in my profile" comment pattern flagged as spam. Dave wrote a clean appeal: acknowledged, no defense, graceful close. Sent.
- **Cost review (last 3 days)** — Apr 3: $23.57 | Apr 4: $49.71 | Apr 5: $77.00 (6AM snapshot). High spend driven by Easter email blast, ZeroBounce, Reddit work, FAL.AI builds.
- **Unified poller built and deployed** — consolidated 5 Stripe delivery pollers (playbook, notion, bundle, aofl, rws) into one script `scripts/unified_poller.py`. Single Stripe call per run. 5 old cron jobs disabled. New `unified_poller` cron live (every 5 min, Haiku 4.5). Est. 80% reduction in poller costs. Committed: 34078d8.
- **Claude Code confirmed dead** — Anthropic sent legal request to remove OAuth in third-party apps (confirmed by OpenCode GitHub commit). 401 auth error on all Claude Code ACP attempts. Not a bug — deliberate enforcement.
- **Codex CLI setup attempted** — API key obtained (sk-proj-llR...), saved to credentials/openai.json and ~/.bashrc. Config written to ~/.codex/config.toml. ACP harness config written to ~/.acpx/config.json. Codex binary located at ~/.npm/_npx/c8ab89660c602c20/.../codex. BLOCKED: OpenAI API account has $0.00 credit balance. Key is valid (curl confirmed 109 models), but Codex uses /v1/responses websocket which requires funded account. Dave needs to add credits at platform.openai.com.
- **GPT-5.4 Mini trial** — gateway-health-monitor cron switched from Haiku 4.5 to GPT-5.4 Mini (openrouter/openai/gpt-5.4-mini) as a trial. Cheaper than Haiku on both input ($0.40 vs $0.80/M) and output ($1.60 vs $4.00/M). Monitoring for quality parity.
- **Formatting rule established** — No markdown tables in replies. Telegram doesn't render them. Use plain bullet lists with bold labels instead.

## New Rules Established

- **No tables in responses** — Telegram renders them as pipe soup. Always use bulleted lists with bold labels.
- **Codex CLI is the preferred coding agent going forward** — replaces Claude Code. Flat cost via ChatGPT Plus (once API credits loaded). ~8-10x cheaper per job than Sonnet 4.6 for script work.
- **Claude Code = dead** — do not attempt ACP harness with agentId "claude". It will 401. Remove from future planning.

## Codex CLI Setup State (INCOMPLETE — needs credits)

- Key: saved to credentials/openai.json and ~/.bashrc
- Config: ~/.codex/config.toml (model=codex-mini-latest, approval_policy=on-failure, preferred_auth_method=apikey)
- ACP harness: ~/.acpx/config.json written with codex agent pointing to npx @openai/codex with key injected
- Binary path: /home/aiboss/.npm/_npx/c8ab89660c602c20/node_modules/@openai/codex-linux-x64/vendor/x86_64-unknown-linux-musl/codex/codex
- **BLOCKER**: $0.00 credit on platform.openai.com API account. Dave needs to add $10-20 at platform.openai.com → Billing → Add credits.
- Once credits added: test with `sessions_spawn(runtime="acp", agentId="codex", ...)`

## Open Items Added This Session

- Add OpenAI API credits ($10-20) at platform.openai.com to unblock Codex CLI
- r/vibecoding appeal sent — monitor for response, consider repost in a few days (clean version, no product links in comments)
- GPT-5.4 Mini trial on gateway-health-monitor — evaluate after 1 week, consider rolling to other Haiku crons if quality holds

## Products / Infrastructure Status

- unified_poller cron: LIVE (job ID: 8b0700c6)
- 5 old pollers: DISABLED
- gateway-health-monitor: GPT-5.4 Mini trial (updated 2026-04-05)
